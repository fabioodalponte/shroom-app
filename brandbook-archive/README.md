# Brandbook Archive

Este diretório contém as páginas de brandbook importadas do Figma.

Status atual:
- Não faz parte do app de gestão (`src/App.tsx` + `src/utils/app-routes.tsx`).
- Mantido apenas como referência histórica de design.

Se quiser reativar no futuro, mova os arquivos de volta para `src/` e reconecte as rotas.
